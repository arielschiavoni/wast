<img src="{{icon}}" alt="" draggable="false">
<div class="carousel-caption">
    <h4>{{name}}</h4>
    <p>Category: <i class="icon-tag"></i><a href="#" class="btn btn-link" data-bypass>{{category}}</a></p>
    <p>
      {{shortDesc}}
    </p>
    <div id="life-cycle"></div>
</div>
